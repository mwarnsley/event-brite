import React from 'react';

const Footer = () => {
    return (
        <footer className="mt-5 p-3 text-center bg-primary">
            Search Events in Your City &copy;
        </footer>
    );
};

export default Footer;
